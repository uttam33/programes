#include<stdio.h>
#include<conio.h>
int main()
{
int a[1000],i,n,sum=0;
printf("enter the size of the arrary : \n");
scanf("%d",&n);
printf("entered the arrary :\n ");
for(i=0;i<n;i++)
{
	scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
	sum+=a[i];
}
printf("the sum is = %d ",sum);
return 0;
}
